{
  "name": "zero-http-lite",
  "version": "0.1.0",
  "private": true,
  "description": "Tiny HTTP request/response primitives written in Zero.",
  "scripts": {
    "smoke": "./scripts/smoke.sh"
  }
}
